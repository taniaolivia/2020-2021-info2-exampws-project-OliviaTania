{% extends 'base.html.twig' %}

{% block title %}Hello VoyageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Détail de voyage</h1>

    <table class="table">
        <thead>
        <tr>
            <th scope="col">Lieu de destination</th>
            <th scope="col">Nom (Participant)</th>
            <th scope="col">Adresse (Participant)</th>
        </tr>
        </thead>
        <tbody>

                <tr>
                    {% for destination in destination %}
                        <th scope="row">{{ destination.lieudestination }}</th>
                    {% endfor %}
                    {% for participant in participant %}
                        <th scope="row">{{ participant.nompart }}</th>
                        <th scope="row">{{ participant.adressepart }}</th>
                    {% endfor %}

                </tr>
        </tbody>
    </table>
</div>
{% endblock %}
